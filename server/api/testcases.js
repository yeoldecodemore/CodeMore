const testCase = {
  backwards_array:
    "it('test1', function(){let value = backwardsArray(3); chai.expect(value).to.equal([3,2,1])}); it('test2', function(){let value = backwardsArray(3); chai.expect(value).to.equal([3,2,1])})",
  count_vowels:
    "it('test1', function(){let value = countVowels('straw warts'); chai.expect(value).to.equal(2)})"
}

module.exports = {
  testCase
}
